suite: excluded failure
templates:
  - templates/deployment.yaml
tests:
  - it: deployment should demonstrate exclusion
    asserts:
      - equal:
          path: metadata.name
          value: excluded-app
