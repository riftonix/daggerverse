suite: service
templates:
  - templates/deployment.yaml
tests:
  - it: deployment should remain selectable through a second glob
    asserts:
      - equal:
          path: metadata.name
          value: passing-app
